import 'package:flutter/cupertino.dart';

class ManufacturerWidget extends StatelessWidget {
  const ManufacturerWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Manufacturer");
  }
}
