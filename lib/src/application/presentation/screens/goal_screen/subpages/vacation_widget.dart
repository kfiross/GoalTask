import 'package:flutter/cupertino.dart';

class VacationWidget extends StatelessWidget{
  const VacationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Vacations");
  }

}